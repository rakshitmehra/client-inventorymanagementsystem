{
  "name": "kitchenstock-api-deploy",
  "private": true,
  "description": "Cloudflare Containers deployment for this service. The API itself is Python - see requirements.txt.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260919.1",
    "typescript": "^5.7.2",
    "wrangler": "^4.135.0"
  }
}
